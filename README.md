# mcdonaldsmenuukprices — Page Package

This package contains 4 files for your target keyword **"how much is a Big Mac"**.

## Files

- **index.html** — The main article page. Luxury/premium black-and-gold theme, custom SVG illustrations (no third-party images, so no copyright risk), 1000+ words, semantic HTML (`h1`/`h2`/`h3`, `<article>`, `<table>`, `<details>` FAQ accordion), `Article` + `FAQPage` JSON-LD schema, and meta title/description tags already filled in.
- **robots.txt** — Allows all crawlers and points to your sitemap.
- **sitemap.xml** — Lists the homepage URL for search engines.
- **README.md** — This file.

## Your anchor link

The anchor text **"Click Here"** appears **once only**, inside the second paragraph, linking to:
`https://mcdsmenuprices.co.uk/mcdonalds-big-mac/`

It does not appear anywhere else on the page, as requested.

## Before you upload

1. **Domain**: I used `https://mcdonaldsmenuukprices.co.uk/` as a placeholder in the `canonical` tag, `robots.txt`, and `sitemap.xml`. If your real domain is different, do a find-and-replace across these three files.
2. **Pricing figures**: The prices quoted in the article are approximate UK ranges compiled from public reporting at the time of writing. McDonald's pricing changes by branch and over time — you may want to spot-check a few current figures on the McDonald's app before publishing, and refresh periodically.
3. **No phone number or call button**: none included, as requested.
4. **Images**: All visuals are hand-coded SVG (burger diagram, ingredient markers, gold dividers) rather than photos, so there's nothing to license or attribute.

## Optional next steps

- Add a real logo in place of the text wordmark in the header.
- Add more internal links once you have more pages on the site.
- Compress/minify the HTML before final upload if your host requires it (not necessary for it to work).
